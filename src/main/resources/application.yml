server:
  port: 8087

spring:
  datasource:
    username: location
    password: location
    url: jdbc:mysql://location.c7e3pqujvfln.us-east-1.rds.amazonaws.com/location
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    show-sql: true